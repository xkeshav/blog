---
import Layout from '../layouts/BlogPost.astro';

const today = new Intl.DateTimeFormat('en-US').format(new Date());
---

<Layout title="Work and Learn" description="work" pubDate={new Date()} updatedDate={new Date()} heroImage="/work.png">
  <p>kuch batayenge thode din me.</p>
</Layout>
